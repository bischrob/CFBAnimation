---
title: "Weekly Post"
output: 
  html_document:
    keep_md: true
date: "`r Sys.Date()`"
---

```{r include=FALSE}
knitr::opts_chunk$set(echo = F)
library(tidyverse)
library(glue)
library(rio)
week = 5
current = import('data/current.xlsx',setclass='tibble')
top = current %>%
  arrange(desc(change)) %>% 
  slice_head(n = 5) %>% 
  select(team,change)
bottom = current %>%
  arrange(change) %>% 
  slice_head(n = 5) %>% 
  select(team,change)
```

[OC] CFB 2023 Weekly Offensive and Defensive Ratings Animated Week `r week`

This is the weekly update for the SP+ CFB rankings included an animated look at the season so far. You can see this week's and prior week's plots [here](https://www.reddit.com/r/CFB/comments/16btt72/oc_cfb_2023_weekly_offensive_and_defensive/?utm_source=share&utm_medium=web2x&context=3).

***

You can see all the charts in one place at this [link](../CFBAnimation/WeeklyPost).

***

## Biggest Movers

Here are the top 5 biggest winners and biggest losers. How did your team do?

```{r}
knitr::kable(top, caption = "Top 5 weekly change",format = 'markdown')
```

```{r}
knitr::kable(bottom, caption = "Bottom 5 weekly change", format = 'markdown')
```

```{r, results = 'asis'}
cat(glue("## All Teams Week {week}"))
```

```{r, results = 'asis'}
cat(glue("![all teams week {week}](../CFBAnimation/figures/week{week}sppcfb.png)"))
```

```{r, results = 'asis'}
cat(glue("## All Teams Week {week} Animated"))
```

```{r, results = 'asis'}
cat(glue("![all teams week {week} animated](../CFBAnimation/figures/CFBEfficiency-week{week}.gif)"))
```

# Conferences

*Prior week's ratings are shown using smaller logos

```{r, results='asis'}
conferences = c("ACC", "American Athletic", "Big 12", "Big Ten", "Conference USA", 
"FBS Independents", "Mid-American", "Mountain West", "Pac-12", 
"SEC", "Sun Belt")
for(conf in conferences){
  cat(glue("\n\n## {conf} week {week}\n\n"),sep = "\n")
  cat(glue("\n\n![{conf} week {week}](../CFBAnimation/figures/week{week}sppcfb-{conf}.png)\n\n"),sep = "\n")
}
```

Let me know what you think!
